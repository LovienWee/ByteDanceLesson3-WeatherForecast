<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- 内容区域 -->
    <FrameLayout
        android:id="@+id/fragment_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- 自定义底部悬浮胶囊 -->
    <LinearLayout
        android:id="@+id/bottom_nav"
        android:layout_width="200dp"
        android:layout_height="56dp"
        android:layout_gravity="center_horizontal|bottom"
        android:layout_marginBottom="16dp"
        android:background="@drawable/bg_bottom_nav_pill"
        android:elevation="8dp"
        android:orientation="horizontal"
        android:padding="4dp">

        <!-- 今天 Tab -->
        <LinearLayout
            android:id="@+id/tab_today"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:orientation="vertical"
            android:gravity="center"
            android:background="@drawable/bg_bottom_nav_item"
            android:foreground="?attr/selectableItemBackgroundBorderless"
            android:clipToOutline="true">

            <TextView
                android:id="@+id/tab_today_icon"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="☀️"
                android:textSize="16sp" />

            <TextView
                android:id="@+id/tab_today_label"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:text="今天"
                android:textColor="#7F3C8CE7"
                android:textSize="14sp"
                android:textStyle="bold" />
        </LinearLayout>

        <!-- 预报 Tab -->
        <LinearLayout
            android:id="@+id/tab_forecast"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:orientation="vertical"
            android:gravity="center"
            android:background="@android:color/transparent"
            android:foreground="?attr/selectableItemBackgroundBorderless"
            android:clipToOutline="true">

            <TextView
                android:id="@+id/tab_forecast_icon"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="📅"
                android:textSize="16sp" />

            <TextView
                android:id="@+id/tab_forecast_label"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:text="预报"
                android:textSize="14sp"
                android:textColor="#80333333" />
        </LinearLayout>

    </LinearLayout>
    </FrameLayout>
